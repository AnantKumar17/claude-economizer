Read the user config at ~/.claude/prompt-economizer/config.json (create it if missing).
If mode is "auto" or missing, set it to "off" and confirm "Prompt Economizer disabled."
If mode is "off", set it to "auto" and confirm "Prompt Economizer enabled (auto mode)."
Write the updated config back to the file.
